# Filename: nct6687d-kmod.spec.in

%if 0%{?fedora}
%global buildforkernels akmod
%endif

%global debug_package %{nil}
%global kmod_name nct6687d

Name:          nct6687d-kmod
Version:       @NCT6687D_VERSION@
Release:       @NCT6687D_RELEASE@
Summary:       NCT6687D kernel module

License:       GPL-2.0-or-later
URL:           https://github.com/Fred78290/nct6687d

Source0:       nct6687d-kmod-%{version}.tar.gz

# NCT6687 is a PC Super-I/O chip: x86 only
ExclusiveArch: x86_64 i686

# get the needed BuildRequires (in parts depending on what we build for)
%global AkmodsBuildRequires %{_bindir}/kmodtool
BuildRequires: %{AkmodsBuildRequires}
BuildRequires: gcc, make

%{!?kernels:BuildRequires: buildsys-build-rpmfusion-kerneldevpkgs-%{?buildforkernels:%{buildforkernels}}%{!?buildforkernels:current}-%{_target_cpu} }

# kmodtool does its magic here
%{expand:%(kmodtool --target %{_target_cpu} --repo rpmfusion --kmodname %{kmod_name} %{?buildforkernels:--%{buildforkernels}} %{?kernels:--for-kernels "%{?kernels}"} 2>/dev/null) }

%description
The NCT6687D %{version} kernel module for kernel %{kversion}.
nct6687d is a driver for the hardware monitoring functionality of
Nuvoton NCT6687 Super-I/O chips, exposing voltage, temperature and fan
sensors through the Linux hwmon subsystem.

%prep
# error out if there was something wrong with kmodtool
%{?kmodtool_check}

# print kmodtool output for debugging purposes:
kmodtool --target %{_target_cpu} --repo rpmfusion --kmodname %{kmod_name} %{?buildforkernels:--%{buildforkernels}} %{?kernels:--for-kernels "%{?kernels}"} 2>/dev/null

%setup -q -c -T -a 0

for kernel_version in %{?kernel_versions} ; do
    cp -a nct6687d-kmod-%{version} _kmod_build_${kernel_version%%___*}
done

%build
for kernel_version in %{?kernel_versions}; do
    pushd _kmod_build_${kernel_version%%___*}/
    make V=0 %{?_smp_mflags} \
        KVER="${kernel_version%%___*}" \
        KDIR="${kernel_version##*___}" \
        modules
    popd
done

%install
for kernel_version in %{?kernel_versions}; do
    mkdir -p $RPM_BUILD_ROOT/%{kmodinstdir_prefix}/${kernel_version%%___*}/%{kmodinstdir_postfix}/
    install -D -m 0644 _kmod_build_${kernel_version%%___*}/nct6687.ko \
         $RPM_BUILD_ROOT/%{kmodinstdir_prefix}/${kernel_version%%___*}/%{kmodinstdir_postfix}/
done
%{?akmod_install}

%changelog
* Wed, 23 Sep 2026 Sunny <yxh9956@gmail.com> - @NCT6687D_VERSION@-@NCT6687D_RELEASE@
- Follow nvidia-kmod pattern for reliable kernel module building
